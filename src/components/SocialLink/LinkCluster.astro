---
import BaseLink from "./BaseLink.astro";
import { meta } from "@/content/meta.ts";
---

<ul>
    {
        meta.social.map((social) => (
            <li>
                <BaseLink icon={social.icon} url={social.link} label={social.label} />
            </li>
        ))
    }
</ul>

<style>
    ul {
        display: flex;
        justify-content: center;
        padding: 0;
        margin: 0.5em;
        gap: 0.5em;
        list-style: none;
    }
</style>
