---
import TechBadge from "@/components/TechBadge.astro";

interface TechSection {
    name: string;
    techList: Array<string>;
}

interface Props {
    value: Array<string | TechSection>;
}

const { value: techList } = Astro.props;
---

{
    techList.map((entry) =>
        typeof entry === "string" ? (
            <TechBadge name={entry} />
        ) : (
            <>
                <p>{entry.name}</p>
                <div class="tech-list">
                    {entry.techList.map((subentry) => (
                        <TechBadge name={subentry} />
                    ))}
                </div>
            </>
        ),
    )
}

<style>
    .tech-list {
        word-spacing: var(--space-xs);
    }
</style>
