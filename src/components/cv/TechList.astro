---
import TechBadge from "@/components/TechBadge.astro";

interface TechSection {
    name: string;
    techList: Array<string>;
}

interface Props {
    value: Array<string | TechSection>;
    simple?: boolean;
}

const { value: techList, simple = false } = Astro.props;
---

{
    techList.map((entry) =>
        typeof entry === "string" ? (
            <TechBadge name={entry} {simple} />
        ) : (
            <>
                <p>{entry.name}</p>
                <div class="tech-list">
                    {entry.techList.map((subentry) => (
                        <TechBadge name={subentry} {simple} />
                    ))}
                </div>
            </>
        ),
    )
}

<style>
    .tech-list {
        word-spacing: var(--space-xs);
    }
</style>
