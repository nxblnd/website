---
import { Icon } from "astro-icon/components";
import { meta } from "@/content/meta.ts";

interface Props {
    showIcons?: boolean;
}
---

<nav>
    <ul>
        {
            meta.menu.map((menuItem) => (
                <li>
                    <a class="primary-link" href={menuItem.path}>
                        {Astro.props.showIcons && menuItem.icon && (
                            <Icon name={menuItem.icon} aria-hidden="true" />
                        )}
                        {menuItem.value}
                    </a>
                </li>
            ))
        }
    </ul>
</nav>

<style>
    nav {
        /* margin-left: auto; */
    }

    ul {
        display: flex;
        flex-flow: row wrap;
        padding: 0;
        gap: 1em;
        list-style: none;
    }

    a {
        display: flex;
        flex-wrap: nowrap;
        align-items: center;
        column-gap: var(--space-sm);
    }

    @media (width < 30rem) {
        nav {
            width: 100%;
        }

        ul {
            width: 100%;
        }
    }
</style>
