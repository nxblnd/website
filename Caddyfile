:80 {
    file_server {
        precompressed
        disable_canonical_uris
    }

    handle_errors {
        rewrite * /{err.status_code}.html
        file_server {
            precompressed
            disable_canonical_uris
        }
    }
}

